# Will need to use:
# https://github.com/containers/skopeo
# To push wo daemon
{
  inputs = {
    nixpkgs.url = github:NixOS/nixpkgs/nixos-unstable;
  };

  outputs = {
    package.default = {
      
    }
  };
}